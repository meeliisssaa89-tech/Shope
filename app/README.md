# Yazan Store - متجر يازان

<div dir="rtl">

## نظرة عامة

**Yazan Store** هو متجر إلكتروني متكامل مصمم للسوق المصري، يدعم اللغة العربية والاتجاه من اليمين لليسار (RTL). يتضمن المشروع واجهة المتجر الأمامية ولوحة تحكم إدارية كاملة في ملف واحد مدمج.

### المميزات الرئيسية

- 🛍️ **واجهة متجر عصرية** - تصميم أنيق ومتجاوب مع جميع الأجهزة
- 🔐 **لوحة تحكم إدارية** - إدارة كاملة للمنتجات والطلبات والفئات
- 💾 **قاعدة بيانات محلية** - تخزين البيانات في LocalStorage (لا يحتاج سيرفر)
- 🌐 **دعم اللغة العربية** - واجهة عربية كاملة مع دعم RTL
- 💳 **طرق دفع متعددة** - كاش، بطاقات، فودافون كاش
- 📱 **تصميم متجاوب** - يعمل على الجوال والتابلت والكمبيوتر

---

## طريقة الوصول للوحة التحكم

### الطريقة الأولى: زر الإعدادات
1. افتح الموقع
2. انقر على زر **الإعدادات** (⚙️) الموجود في الزاوية اليسرى السفلية
3. اختر **لوحة التحكم**

### الطريقة الثانية: القائمة الجانبية
1. افتح الموقع على الجوال
2. انقر على أيقونة **القائمة** (☰) في الأعلى
3. اختر **لوحة التحكم** من أسفل القائمة

### بيانات تسجيل الدخول الافتراضية
```
البريد الإلكتروني: admin@yazanstore.com
كلمة المرور: admin123
```

---

## تعليمات التثبيت

### المتطلبات

- Node.js 18+ 
- npm أو yarn

### خطوات التثبيت

#### 1. استنساخ المشروع

```bash
cd /mnt/okcomputer/output/app
```

#### 2. تثبيت التبعيات

```bash
npm install
```

#### 3. تشغيل المشروع في وضع التطوير

```bash
npm run dev
```

سيعمل الموقع على: `http://localhost:5173`

#### 4. بناء المشروع للإنتاج

```bash
npm run build
```

سيتم إنشاء مجلد `dist` يحتوي على الملفات الجاهزة للنشر.

#### 5. معاينة البناء

```bash
npm run preview
```

---

## طريقة النشر على الاستضافة

### النشر على Netlify

1. قم ببناء المشروع:
```bash
npm run build
```

2. ارفع مجلد `dist` إلى Netlify

3. أو استخدم Netlify CLI:
```bash
npm install -g netlify-cli
netlify deploy --prod --dir=dist
```

### النشر على Vercel

1. قم ببناء المشروع:
```bash
npm run build
```

2. ارفع مجلد `dist` إلى Vercel

3. أو استخدم Vercel CLI:
```bash
npm install -g vercel
vercel --prod
```

### النشر على أي استضافة ويب

1. قم ببناء المشروع:
```bash
npm run build
```

2. ارفع محتويات مجلد `dist` إلى مجلد `public_html` على الاستضافة

3. تأكد من وجود ملف `index.html` في الجذر

---

## هيكل المشروع

```
app/
├── src/
│   ├── admin/           # لوحة التحكم الإدارية
│   │   ├── components/  # مكونات لوحة التحكم
│   │   └── pages/       # صفحات لوحة التحكم
│   ├── components/      # المكونات المشتركة
│   │   ├── custom/      # مكونات مخصصة
│   │   └── ui/          # مكونات shadcn/ui
│   ├── hooks/           # React Hooks
│   ├── lib/             # المكتبات والأدوات
│   │   ├── db/          # قاعدة البيانات (LocalStorage)
│   │   └── utils.ts     # دوال مساعدة
│   ├── sections/        # أقسام الصفحة الرئيسية
│   ├── store/           # إدارة الحالة (Context)
│   ├── types/           # أنواع TypeScript
│   ├── App.tsx          # الملف الرئيسي
│   └── main.tsx         # نقطة الدخول
├── public/              # الملفات العامة
├── index.html
├── package.json
├── tailwind.config.js
├── tsconfig.json
└── vite.config.ts
```

---

## قاعدة البيانات

المشروع يستخدم **LocalStorage** كقاعدة بيانات محلية، مما يعني:

- ✅ لا يحتاج إلى سيرفر خلفي
- ✅ البيانات تُخزن في متصفح المستخدم
- ⚠️ البيانات تُحذف عند مسح ذاكرة المتصفح
- ⚠️ البيانات لا تتزامن بين الأجهزة

### التراكيب المخزنة

```typescript
// المنتجات
interface Product {
  id: string;
  name: string;
  description: string;
  price: number;
  originalPrice?: number;
  categoryId: string;
  images: string[];
  sizes: string[];
  colors: string[];
  stock: number;
  isNew?: boolean;
  isOnSale?: boolean;
  isFeatured?: boolean;
}

// الفئات
interface Category {
  id: string;
  name: string;
  description: string;
  image: string;
  productCount: number;
}

// الطلبات
interface Order {
  id: string;
  customerName: string;
  phone: string;
  address: string;
  city: string;
  items: OrderItem[];
  total: number;
  shippingCost: number;
  paymentMethod: 'cod' | 'card' | 'vodafone';
  status: 'pending' | 'processing' | 'shipped' | 'delivered' | 'cancelled';
  notes?: string;
  createdAt: string;
}

// الإعدادات
interface Settings {
  storeName: string;
  storeDescription: string;
  phone: string;
  whatsapp: string;
  email: string;
  address: string;
  shippingCost: number;
  freeShippingThreshold: number;
  currency: string;
}
```

---

## ربط قاعدة بيانات حقيقية

لربط المشروع بقاعدة بيانات حقيقية، يمكنك:

### 1. Firebase (الأسهل)

```bash
npm install firebase
```

ثم عدل ملف `src/lib/db/index.ts` ليستخدم Firebase بدلاً من LocalStorage.

### 2. Supabase

```bash
npm install @supabase/supabase-js
```

### 3. MongoDB + Express

أنشئ سيرفر Express منفصل وربطه بالمشروع.

---

## تخصيص المشروع

### تغيير بيانات تسجيل الدخول

عدل ملف `src/store/adminStore.tsx`:

```typescript
const DEFAULT_ADMIN = {
  email: 'admin@yazanstore.com',
  password: 'admin123', // غير كلمة المرور هنا
  name: 'المشرف'
};
```

### تغيير معلومات المتجر

يمكنك تغيير معلومات المتجر من لوحة التحكم > الإعدادات.

### إضافة منتجات

يمكنك إضافة منتجات من لوحة التحكم > المنتجات.

---

## الأوامر المتاحة

| الأمر | الوصف |
|-------|-------|
| `npm run dev` | تشغيل في وضع التطوير |
| `npm run build` | بناء المشروع للإنتاج |
| `npm run preview` | معاينة البناء |
| `npm run lint` | فحص الأخطاء |

---

## التقنيات المستخدمة

- **React 19** - مكتبة واجهات المستخدم
- **TypeScript** - لغة البرمجة
- **Vite** - أداة البناء
- **Tailwind CSS** - إطار عمل CSS
- **shadcn/ui** - مكونات واجهة المستخدم
- **Lucide React** - أيقونات
- **Recharts** - رسوم بيانية

---

## المساعدة والدعم

إذا واجهت أي مشكلة:

1. تأكد من استخدام Node.js 18+
2. احذف مجلد `node_modules` وأعد التثبيت:
   ```bash
   rm -rf node_modules package-lock.json
   npm install
   ```
3. تأكد من عدم وجود عمليات أخرى تستخدم نفس المنفذ

---

## الترخيص

هذا المشروع مفتوح المصدر ومتاح للاستخدام الشخصي والتجاري.

---

<div align="center">

**صنع بـ ❤️ لمتجر يازان**

</div>

</div>
